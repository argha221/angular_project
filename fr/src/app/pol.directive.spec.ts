import { PolDirective } from './pol.directive';

describe('PolDirective', () => {
  it('should create an instance', () => {
    const directive = new PolDirective();
    expect(directive).toBeTruthy();
  });
});
